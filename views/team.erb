<!DOCTYPE html>
<html>
 <head>
  <meta charset="UTF-8">
  <title>Basketball Team</title>
 </head>
 <body>
   <%= params %>
   <p>Team Name:<%= params[:name] %></p>
   <p>Coach:</p>
  <p>Point Guard:</p>
  <p>Shooting Guard:</p>
  <p>Power Forward:</p>
  <p>Small Forward:</p>

 <% params.each do |key, value| %>
 <p> <%= "#{key}: #{value}" %></p>
 <% end %>


 </body>
</html>
